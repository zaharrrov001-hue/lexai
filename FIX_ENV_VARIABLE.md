# 🔧 Исправление переменной окружения OPENAI_API_KEY

## ❌ Проблема

В Mastra Cloud создана переменная `openai1`, но нужна `OPENAI_API_KEY` (точное имя).

## ✅ Решение

### Вариант 1: Переименовать существующую переменную (рекомендуется)

1. **Откройте Project Settings → Env Variables**
   - URL: `cloud.mastra.ai/.../runtime/settings?tab=env-vars`

2. **Найдите переменную `openai1`**

3. **Удалите её** (нажмите на иконку удаления/крестик)

4. **Создайте новую переменную:**
   - **Key:** `OPENAI_API_KEY` (точное имя, заглавными буквами)
   - **Value:** `sk-proj-6Cvqqg_zeheoV43...` (ваш ключ)
   - **Sensitive:** ✅ Отметьте галочку (чтобы скрыть значение)

5. **Нажмите "Save variables"**

6. **Перезапустите проект:**
   - Перейдите в **Deployments**
   - Нажмите **"Redeploy"** или **"Restart"**

### Вариант 2: Добавить новую переменную (если хотите оставить openai1)

1. **В разделе "Add Environment Variables":**
   - **Key:** `OPENAI_API_KEY`
   - **Value:** ваш ключ OpenAI (скопируйте из `openai1`)
   - **Sensitive:** ✅ Отметьте галочку

2. **Нажмите "Save variables"**

3. **Перезапустите проект**

## ⚠️ Важно

- **Имя переменной должно быть точно:** `OPENAI_API_KEY` (заглавными буквами)
- **Не:** `openai1`, `OPENAI_KEY`, `openai_api_key` и т.д.
- После сохранения **обязательно перезапустите проект**

## ✅ Проверка

После перезапуска:
1. Откройте **Agents → LexAI Agent**
2. Предупреждение должно исчезнуть
3. В разделе "Model" должно быть: "OpenAI / gpt-4o-mini" без предупреждений

## 📋 Пошаговая инструкция с картинками:

### Шаг 1: Удалить старую переменную
- Найдите `openai1` в списке
- Нажмите на иконку удаления (крестик или корзина)

### Шаг 2: Добавить новую переменную
```
Key: OPENAI_API_KEY
Value: sk-proj-6Cvqqg_zeheoV43... (ваш ключ)
☑ Sensitive
```

### Шаг 3: Сохранить
- Нажмите **"Save variables"**

### Шаг 4: Перезапустить
- Перейдите в **Deployments**
- Нажмите **"Redeploy"** или найдите кнопку перезапуска

## 🔍 Если не помогло

1. Проверьте, что имя переменной точно `OPENAI_API_KEY` (без пробелов)
2. Убедитесь, что значение ключа полное (начинается с `sk-`)
3. Проверьте логи в разделе **Deployments → Logs**
4. Попробуйте полностью пересобрать проект (Redeploy)

