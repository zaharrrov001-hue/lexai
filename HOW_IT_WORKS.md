# 🎯 Как работает LexAI Mastra Agent

## 📋 Обзор проекта

Это **Mastra Agent** - AI агент для юридических задач, который развертывается на Mastra Cloud и предоставляет API для взаимодействия.

---

## 🏗️ Архитектура проекта

### Структура файлов:

```
mastra/
├── src/mastra/
│   ├── index.ts              # 🎯 ТОЧКА ВХОДА - создает Mastra instance
│   ├── agents/
│   │   └── lexai-agent.ts    # 🤖 АГЕНТ - основной AI агент
│   └── tools/
│       ├── web-search-tool.ts         # 🔍 Инструмент веб-поиска
│       ├── document-analysis-tool.ts # 📄 Инструмент анализа документов
│       ├── legal-query-tool.ts       # ⚖️ Инструмент юридических запросов
│       └── index.ts                   # Экспорт всех инструментов
├── package.json              # Зависимости и скрипты
└── tsconfig.json             # TypeScript конфигурация
```

---

## 🔄 Как это работает

### 1. **Точка входа** (`src/mastra/index.ts`)

```typescript
import { Mastra } from "@mastra/core/mastra";
import { lexaiAgent } from "./agents/lexai-agent";

export const mastra = new Mastra({
  agents: { lexaiAgent },
});
```

**Что делает:**
- Создает **Mastra instance** - главный объект приложения
- Регистрирует **lexaiAgent** - делает его доступным через API
- При развертывании Mastra Cloud автоматически находит этот файл и создает HTTP сервер

---

### 2. **Агент** (`src/mastra/agents/lexai-agent.ts`)

```typescript
export const lexaiAgent = new Agent({
  name: "LexAI Agent",
  instructions: "Вы - LexAI, профессиональный AI-ассистент...",
  model: "openai/gpt-4o-mini",
  tools: {
    webSearchTool,
    documentAnalysisTool,
    legalQueryTool,
  },
});
```

**Что делает:**
- Определяет **поведение агента** через `instructions` (системный промпт)
- Указывает **модель LLM** для использования (`openai/gpt-4o-mini`)
- Подключает **инструменты** (tools) - функции, которые агент может вызывать
- Ограничивает **максимальное количество шагов** (`maxSteps: 10`)

**Как работает:**
1. Пользователь отправляет запрос
2. Агент анализирует запрос
3. Решает, какие инструменты использовать
4. Вызывает инструменты
5. Формирует ответ на основе результатов

---

### 3. **Инструменты** (`src/mastra/tools/`)

#### 🔍 Web Search Tool
```typescript
export const webSearchTool = createTool({
  id: "web-search",
  description: "Ищет информацию в интернете...",
  inputSchema: z.object({
    query: z.string(),
    limit: z.number().optional(),
  }),
  execute: async ({ query, limit }) => {
    // Логика поиска
  },
});
```

**Что делает:**
- Принимает поисковый запрос
- Возвращает результаты поиска
- Агент использует его, когда нужна актуальная информация

#### 📄 Document Analysis Tool
```typescript
export const documentAnalysisTool = createTool({
  id: "document-analysis",
  description: "Анализирует документы...",
  // ...
});
```

**Что делает:**
- Анализирует документы (PDF, DOCX, TXT)
- Извлекает ключевую информацию
- Создает резюме

#### ⚖️ Legal Query Tool
```typescript
export const legalQueryTool = createTool({
  id: "legal-query",
  description: "Обрабатывает юридические запросы...",
  // ...
});
```

**Что делает:**
- Обрабатывает юридические вопросы
- Предоставляет информацию о законах
- Оценивает риски

---

## 🚀 Процесс работы

### Локальная разработка:

```bash
npm run dev
```

**Что происходит:**
1. Запускается Mastra dev сервер на `http://localhost:4111`
2. Открывается **Studio** - веб-интерфейс для тестирования
3. Можно тестировать агента через UI

### Развертывание на Mastra Cloud:

```bash
npm run build
```

**Что происходит:**
1. Mastra анализирует зависимости
2. Бандлит код в `.mastra/output/`
3. Создает готовый к развертыванию сервер
4. После загрузки в Mastra Cloud создается HTTP API

---

## 📡 API Endpoints

После развертывания доступны:

### 1. Генерация ответа (не streaming)
```
POST /api/agents/lexaiAgent/generate
Content-Type: application/json

{
  "messages": [
    {
      "role": "user",
      "content": "Проанализируй этот договор..."
    }
  ]
}
```

**Ответ:**
```json
{
  "text": "Ответ агента...",
  "toolResults": [...],
  "usage": {...}
}
```

### 2. Streaming ответа
```
POST /api/agents/lexaiAgent/stream
```

**Ответ:** Поток токенов в реальном времени

---

## 🔄 Поток выполнения запроса

```
1. Пользователь → HTTP запрос
   ↓
2. Mastra Cloud → Принимает запрос
   ↓
3. Mastra Instance → Находит lexaiAgent
   ↓
4. LexAI Agent → Анализирует запрос
   ↓
5. Agent → Решает какие инструменты использовать
   ↓
6. Tools → Выполняются (webSearch, documentAnalysis, legalQuery)
   ↓
7. Agent → Получает результаты от инструментов
   ↓
8. Agent → Формирует финальный ответ
   ↓
9. HTTP Response → Возвращается пользователю
```

---

## 🛠️ Инструменты в действии

### Пример 1: Поиск информации

**Запрос:** "Найди информацию о трудовом кодексе"

**Процесс:**
1. Агент видит запрос о поиске
2. Вызывает `webSearchTool` с запросом "трудовой кодекс"
3. Получает результаты
4. Формирует ответ на основе результатов

### Пример 2: Анализ документа

**Запрос:** "Проанализируй этот договор: [текст]"

**Процесс:**
1. Агент видит запрос на анализ
2. Вызывает `documentAnalysisTool` с текстом договора
3. Инструмент извлекает ключевую информацию
4. Агент формирует структурированный ответ

### Пример 3: Юридический вопрос

**Запрос:** "Какие права у работника при увольнении?"

**Процесс:**
1. Агент видит юридический вопрос
2. Вызывает `legalQueryTool` с вопросом
3. Инструмент обрабатывает запрос
4. Агент формирует профессиональный ответ

---

## ⚙️ Конфигурация

### package.json
```json
{
  "scripts": {
    "dev": "mastra dev",      // Запуск dev сервера
    "build": "mastra build",  // Сборка для production
    "start": "node dist/index.js"  // Запуск собранного сервера
  }
}
```

### tsconfig.json
- `"module": "ES2022"` - современные ES модули
- `"moduleResolution": "bundler"` - для Mastra (обязательно!)
- `"target": "ES2022"` - совместимость

---

## 🎯 Ключевые особенности

### 1. **Автоматическая маршрутизация инструментов**
Агент сам решает, какой инструмент использовать для каждого запроса.

### 2. **Type-safe инструменты**
Все инструменты используют Zod схемы для валидации входных/выходных данных.

### 3. **Многошаговые задачи**
Агент может выполнять несколько шагов подряд (`maxSteps: 10`).

### 4. **Структурированные ответы**
Инструменты возвращают структурированные данные, которые агент использует для формирования ответа.

---

## 📊 Примеры использования

### Через HTTP API:

```bash
curl -X POST https://your-project.mastra.ai/api/agents/lexaiAgent/generate \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [
      {
        "role": "user",
        "content": "Найди информацию о гражданском праве"
      }
    ]
  }'
```

### Через Mastra Studio:

1. Откройте Studio в Mastra Cloud
2. Выберите `lexaiAgent`
3. Отправьте сообщение
4. Увидите ответ и traces (что делал агент)

---

## ✅ Что проверено

- ✅ Структура проекта правильная
- ✅ Entry point существует
- ✅ Агент зарегистрирован
- ✅ Инструменты работают
- ✅ Сборка проходит успешно
- ✅ MCP интеграция удалена (нужна только для Cursor IDE)

---

## 🚀 Готово к развертыванию!

Проект полностью готов. Все работает правильно.

**Следующий шаг:** Разверните на Mastra Cloud! 🎉

