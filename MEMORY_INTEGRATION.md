# 🧠 Memory Integration - LexAI Mastra Agent

## ✅ Memory интегрирована!

Система Memory полностью интегрирована в LexAI Mastra Agent. Агент теперь может:
- ✅ Помнить историю разговоров
- ✅ Искать по прошлым разговорам (semantic recall)
- ✅ Хранить постоянную информацию о пользователе (working memory)

---

## 📋 Что настроено

### 1. Storage (Хранилище)

```typescript
// src/mastra/index.ts
export const mastra = new Mastra({
  agents: { lexaiAgent },
  storage: new LibSQLStore({
    url: process.env.DATABASE_URL || "file:./lexai-memory.db",
  }),
});
```

**Что делает:**
- Хранит все сообщения, треды и working memory
- Использует LibSQL (SQLite) для локальной разработки
- Можно переключить на PostgreSQL через `DATABASE_URL`

---

### 2. Memory в агенте

```typescript
// src/mastra/agents/lexai-agent.ts
memory: new Memory({
  storage: new LibSQLStore({...}),
  vector: new LibSQLVector({...}),
  embedder: "openai/text-embedding-3-small",
  options: {
    lastMessages: 20,              // История разговора
    semanticRecall: {...},         // Поиск по прошлым разговорам
    workingMemory: {...},          // Постоянная память о пользователе
    threads: { generateTitle: true }, // Автогенерация заголовков
  },
})
```

---

## 🎯 Типы Memory

### 1. Conversation History (История разговора)

**Что это:**
- Последние 20 сообщений из текущего разговора
- Обеспечивает краткосрочный контекст

**Настройка:**
```typescript
lastMessages: 20
```

**Использование:**
- Агент видит последние 20 сообщений в каждом запросе
- Автоматически включается в контекст

---

### 2. Semantic Recall (Семантический поиск)

**Что это:**
- Поиск релевантных сообщений из прошлых разговоров
- Использует векторные embeddings для поиска по смыслу

**Настройка:**
```typescript
semanticRecall: {
  topK: 5,              // Найти 5 наиболее релевантных сообщений
  messageRange: 2,     // Включить 2 сообщения до и после найденного
  scope: "resource",   // Поиск по всем разговорам пользователя
}
```

**Как работает:**
1. Новое сообщение конвертируется в embedding
2. Ищутся похожие сообщения в векторной БД
3. Найденные сообщения включаются в контекст

**Пример:**
- Пользователь спрашивает: "Что я спрашивал про трудовой кодекс?"
- Агент находит прошлые сообщения про трудовой кодекс
- Использует их для ответа

---

### 3. Working Memory (Рабочая память)

**Что это:**
- Постоянная информация о пользователе
- Сохраняется между разговорами
- Агент может обновлять через специальный инструмент

**Настройка:**
```typescript
workingMemory: {
  enabled: true,
  scope: "resource",  // Память сохраняется между разговорами
  template: `# Профиль пользователя
## Личная информация
- Имя:
- Роль/Должность:
...
`,
}
```

**Шаблон включает:**
- Личную информацию (имя, роль, компания)
- Юридические предпочтения (область права, стиль общения)
- Текущий контекст (задача, детали, сроки)
- Историю взаимодействий

**Как работает:**
1. Агент видит шаблон в начале каждого разговора
2. Когда пользователь сообщает информацию (имя, предпочтения), агент обновляет working memory
3. В следующих разговорах агент помнит эту информацию

**Пример:**
- Пользователь: "Меня зовут Иван, я работаю юристом в IT-компании"
- Агент обновляет working memory: `Имя: Иван, Роль: Юрист, Компания: IT-компания`
- В следующих разговорах агент будет обращаться: "Иван, как юрист в IT-компании..."

---

## 🔄 Как использовать Memory

### При вызове агента

```typescript
const response = await lexaiAgent.generate("Вопрос пользователя", {
  memory: {
    thread: "thread-123",      // ID разговора (уникальный для каждого разговора)
    resource: "user-456",      // ID пользователя (один для всех разговоров пользователя)
  },
});
```

**Важно:**
- `thread` - уникальный для каждого разговора (создавайте новый для нового разговора)
- `resource` - один для всех разговоров пользователя (используйте user ID)

### Создание нового разговора

```typescript
const memory = await lexaiAgent.getMemory();
const thread = await memory?.createThread({
  resourceId: "user-456",
  title: "Вопрос о трудовом кодексе", // Опционально, можно автогенерировать
});

const response = await lexaiAgent.generate("Вопрос", {
  memory: {
    thread: thread.id,
    resource: thread.resourceId,
  },
});
```

### Продолжение разговора

```typescript
// Используйте тот же thread ID
const response = await lexaiAgent.generate("Следующий вопрос", {
  memory: {
    thread: "thread-123",  // Тот же thread
    resource: "user-456",  // Тот же resource
  },
});
```

---

## 📊 Что сохраняется

### В Storage (LibSQL):
- **Треды** (threads) - информация о разговорах
- **Сообщения** (messages) - все сообщения пользователя и агента
- **Working Memory** - постоянная информация о пользователе

### В Vector Store (LibSQLVector):
- **Embeddings** - векторные представления сообщений для semantic recall

---

## 🎯 Преимущества Memory

1. **Контекст между сообщениями**
   - Агент помнит, о чем говорили ранее в разговоре
   - Не нужно повторять информацию

2. **Поиск по прошлым разговорам**
   - Агент может найти релевантную информацию из прошлых разговоров
   - Полезно для сложных вопросов

3. **Персонализация**
   - Агент помнит предпочтения пользователя
   - Может адаптировать стиль общения

4. **Автоматические заголовки**
   - Каждый разговор получает понятный заголовок
   - Легче навигировать по истории

---

## 🔧 Настройка для Production

### Использование PostgreSQL

```typescript
import { PgStore, PgVector } from "@mastra/pg";

export const mastra = new Mastra({
  agents: { lexaiAgent },
  storage: new PgStore({
    connectionString: process.env.DATABASE_URL,
  }),
});

// В агенте:
memory: new Memory({
  storage: new PgStore({
    connectionString: process.env.DATABASE_URL,
  }),
  vector: new PgVector({
    connectionString: process.env.DATABASE_URL,
  }),
  // ...
})
```

### Environment Variables

```env
# Обязательно
OPENAI_API_KEY=your-openai-api-key

# Опционально (для PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/dbname
```

---

## ✅ Проверка работы

### Локально

```bash
cd mastra
npm run dev
```

Откройте Studio на `http://localhost:4111` и проверьте:
- История разговоров сохраняется
- Working memory обновляется
- Semantic recall работает

### В Production

После развертывания на Mastra Cloud:
- Memory работает автоматически
- Все данные сохраняются в storage
- Можно проверить через API или Studio

---

## 📚 Документация

- [Mastra Memory Overview](https://mastra.ai/docs/memory/overview)
- [Conversation History](https://mastra.ai/docs/memory/conversation-history)
- [Semantic Recall](https://mastra.ai/docs/memory/semantic-recall)
- [Working Memory](https://mastra.ai/docs/memory/working-memory)

---

**Memory полностью интегрирована и готова к использованию! 🎉**

