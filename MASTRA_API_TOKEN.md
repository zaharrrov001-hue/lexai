# 🔐 Mastra Cloud API Token - Управление проектом

## ✅ Да, через этот токен можно управлять проектом!

JWT токен, который вы предоставили, содержит:
- **teamId**: `53859133-3cfd-48f3-9b99-b86ea828ae8e`
- **projectId**: `6b16d5b0-b615-4266-8570-9e25056268b3` (ваш проект LexAI)
- **uniqueId**: `6c72f30a-edc2-4e5b-8857-ee0eaedf7b1f`

## 🎯 Что можно делать через API:

### 1. **Управление переменными окружения**
   - Установить `OPENAI_API_KEY` и другие переменные
   - Просмотреть все переменные окружения
   - Обновить существующие переменные

### 2. **Управление деплоями**
   - Перезапустить проект
   - Проверить статус деплоя
   - Просмотреть логи сборки и выполнения

### 3. **Мониторинг**
   - Получить логи приложения
   - Проверить статус проекта
   - Просмотреть метрики

## ⚠️ Важно:

1. **Безопасность токена:**
   - ❌ Не публикуйте токен в открытых репозиториях
   - ❌ Не делитесь токеном с третьими лицами
   - ✅ Храните токен в переменных окружения
   - ✅ Используйте `.env` файл (добавлен в `.gitignore`)

2. **Права доступа:**
   - Токен может иметь ограниченные права
   - Некоторые операции могут требовать дополнительных разрешений
   - Проверьте документацию Mastra Cloud API для точных endpoints

## 🚀 Использование:

### Вариант 1: Через скрипт (если API доступен)

```bash
# Установить переменную окружения
node manage-mastra-cloud.js set-env OPENAI_API_KEY sk-your-key-here

# Получить все переменные
node manage-mastra-cloud.js get-env

# Перезапустить проект
node manage-mastra-cloud.js restart

# Проверить статус
node manage-mastra-cloud.js status

# Получить логи
node manage-mastra-cloud.js logs
```

### Вариант 2: Через веб-интерфейс (рекомендуется)

1. Откройте https://cloud.mastra.ai/
2. Войдите в аккаунт
3. Откройте проект `6b16d5b0-b615-4266-8570-9e25056268b3`
4. Перейдите в **Settings** → **Environment Variables**
5. Добавьте `OPENAI_API_KEY`

### Вариант 3: Через REST API напрямую

```bash
# Установить переменную окружения
curl -X PUT "https://cloud.mastra.ai/api/projects/6b16d5b0-b615-4266-8570-9e25056268b3/env" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json" \
  -d '{"OPENAI_API_KEY": "sk-your-key-here"}'
```

## 📚 Документация API:

- Mastra Cloud API: https://docs.mastra.ai/cloud/api (если доступна)
- JWT Token Info: Токен содержит информацию о проекте и команде

## 🔍 Проверка токена:

Токен можно декодировать для просмотра содержимого:

```javascript
const token = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
const payload = JSON.parse(Buffer.from(token.split('.')[1], 'base64').toString());
console.log(payload);
// {
//   teamId: '53859133-3cfd-48f3-9b99-b86ea828ae8e',
//   projectId: '6b16d5b0-b615-4266-8570-9e25056268b3',
//   uniqueId: '6c72f30a-edc2-4e5b-8857-ee0eaedf7b1f',
//   iat: 1766167198
// }
```

## ⚡ Быстрое решение проблемы с OPENAI_API_KEY:

Если API доступен, можно установить переменную автоматически:

```bash
export MASTRA_CLOUD_TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
export OPENAI_API_KEY="sk-your-openai-key-here"

node manage-mastra-cloud.js set-env OPENAI_API_KEY "$OPENAI_API_KEY"
node manage-mastra-cloud.js restart
```

**Примечание:** Точные API endpoints могут отличаться. Проверьте документацию Mastra Cloud или используйте веб-интерфейс для гарантированной работы.

