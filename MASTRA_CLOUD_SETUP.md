# Настройка Mastra Cloud для LexAI

## ✅ Исправление загружено на GitHub

Проблема с отсутствующим `OPENAI_API_KEY` исправлена. Теперь `embedder` и `semanticRecall` опциональны и включаются только при наличии API ключа.

## 🔧 Настройка переменных окружения в Mastra Cloud

После импорта проекта из GitHub необходимо настроить переменные окружения:

### Обязательные переменные:

1. **OPENAI_API_KEY** (обязательно для работы Memory с semantic recall)
   - Получите ключ на https://platform.openai.com/api-keys
   - Формат: `sk-...`
   - Без этого ключа Memory будет работать только с `lastMessages` (без semantic recall)

### Опциональные переменные:

2. **DATABASE_URL** (опционально)
   - По умолчанию: `file:./lexai-agent-memory.db`
   - Для production рекомендуется использовать внешнюю БД:
     - PostgreSQL: `postgresql://user:password@host:5432/dbname`
     - LibSQL (Turso): `libsql://your-database.turso.io`

3. **PORT** (опционально)
   - По умолчанию: `4111`
   - Mastra Cloud обычно устанавливает это автоматически

4. **CORS_ORIGIN** (опционально)
   - По умолчанию: `*` (разрешены все источники)
   - Для production укажите конкретные домены через запятую

## 📝 Как добавить переменные в Mastra Cloud:

1. Откройте ваш проект в Mastra Cloud
2. Перейдите в раздел **Settings** → **Environment Variables**
3. Добавьте `OPENAI_API_KEY` со значением вашего ключа
4. Сохраните изменения
5. Перезапустите проект (или дождитесь автоматического перезапуска)

## 🔍 Проверка работы:

После добавления `OPENAI_API_KEY`:
- ✅ Memory будет работать с полным функционалом (semantic recall включен)
- ✅ Embedder будет использовать `text-embedding-3-small` для векторного поиска
- ✅ Агент сможет находить релевантные сообщения из прошлых разговоров

Без `OPENAI_API_KEY`:
- ⚠️ Memory будет работать только с `lastMessages` (последние 20 сообщений)
- ⚠️ Semantic recall будет отключен
- ✅ Агент все равно будет работать, но без поиска по истории

## 🚀 После настройки:

1. Mastra Cloud автоматически пересоберет проект
2. Readiness probe должен пройти успешно
3. Агент будет доступен через API Mastra Cloud

## 📚 Документация:

- Mastra Cloud: https://cloud.mastra.ai/
- Memory System: https://docs.mastra.ai/memory/
- Environment Variables: https://docs.mastra.ai/deployment/

