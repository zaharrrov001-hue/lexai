# 🎯 Настройка Mastra Docs MCP Server для Cursor

## ✅ Глобальная настройка для всех проектов

Эта инструкция поможет настроить Mastra Docs MCP Server в Cursor IDE, чтобы он был доступен **во всех ваших проектах** при работе с Mastra.

---

## 🚀 Быстрая установка (рекомендуется)

### Вариант 1: Автоматическая установка (один клик)

Нажмите на кнопку ниже для автоматической установки:

[![Install MCP Server](https://cursor.com/deeplink/mcp-install-light.svg)](cursor://anysphere.cursor-deeplink/mcp/install?name=mastra&config=eyJjb21tYW5kIjoibnB4IC15IEBtYXN0cmEvbWNwLWRvY3Mtc2VydmVyIn0%3D)

После установки:
1. Откройте Cursor
2. В левом нижнем углу появится уведомление
3. Нажмите **"Enable"** для активации Mastra Docs MCP Server

---

## 🔧 Ручная настройка

### Шаг 1: Найти файл конфигурации MCP в Cursor

Файл конфигурации MCP находится в:
- **macOS/Linux**: `~/.cursor/mcp.json` или `~/.cursor/mcp_config.json`
- **Windows**: `%APPDATA%\Cursor\mcp.json` или `%APPDATA%\Cursor\mcp_config.json`

### Шаг 2: Создать/отредактировать конфигурацию

Создайте файл `mcp.json` (если его нет) или откройте существующий:

```json
{
  "mcpServers": {
    "mastra-docs": {
      "command": "npx",
      "args": ["-y", "@mastra/mcp-docs-server"]
    }
  }
}
```

### Шаг 3: Перезапустить Cursor

1. Полностью закройте Cursor
2. Откройте заново
3. MCP сервер должен автоматически запуститься

---

## ✅ Проверка установки

### Способ 1: Через настройки Cursor

1. Откройте настройки Cursor: `Cmd/Ctrl + ,`
2. Найдите раздел **"MCP"** или **"Model Context Protocol"**
3. Убедитесь, что **"Mastra Docs"** или **"mastra-docs"** отображается как активный

### Способ 2: Через чат Cursor

1. Откройте чат в Cursor
2. Спросите: "Как использовать Agent в Mastra?"
3. Если Cursor использует Mastra Docs MCP, он найдет актуальную информацию из документации

### Способ 3: Проверка в логах

1. Откройте Developer Tools в Cursor: `Cmd/Ctrl + Shift + I`
2. Проверьте консоль на наличие ошибок MCP
3. Должны быть сообщения о подключении к `@mastra/mcp-docs-server`

---

## 🎯 Что это дает

После настройки, **в любом проекте** Cursor будет:

✅ **Использовать актуальную документацию Mastra** при создании агентов  
✅ **Показывать правильные примеры кода** из официальной документации  
✅ **Знать последние изменения** в API Mastra  
✅ **Предлагать best practices** из официальных гайдов  

### Примеры использования:

```
Вы: "Создай агента Mastra с memory"
Cursor: Использует Mastra Docs MCP → находит актуальную документацию → создает правильный код
```

```
Вы: "Добавь workflow в Mastra проект"
Cursor: Использует Mastra Docs MCP → находит примеры → добавляет правильную структуру
```

```
Вы: "Как использовать tools в Mastra?"
Cursor: Использует Mastra Docs MCP → находит документацию → объясняет с примерами
```

---

## 🔄 Обновление MCP сервера

MCP сервер обновляется автоматически при каждом использовании через `npx -y`, но можно принудительно обновить:

```bash
npx -y @mastra/mcp-docs-server@latest
```

Или просто перезапустите Cursor - он автоматически загрузит последнюю версию.

---

## ⚠️ Решение проблем

### Проблема 1: MCP сервер не запускается

**Решение:**
1. Проверьте что `npx` установлен: `npx --version`
2. Проверьте синтаксис JSON в конфигурации
3. Перезапустите Cursor

### Проблема 2: Cursor не использует документацию

**Решение:**
1. Убедитесь что MCP сервер активен в настройках
2. Проверьте что вы в режиме Agent/Chat в Cursor
3. Попробуйте явно попросить: "Используй документацию Mastra для..."

### Проблема 3: Ошибки подключения

**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь что нет блокировки `npx`
3. Проверьте логи в Developer Tools

---

## 📚 Дополнительная информация

- [Официальная документация Mastra Docs MCP](https://mastra.ai/docs/getting-started/mcp-docs-server)
- [Cursor MCP Documentation](https://cursor.com/de/docs/context/mcp)
- [Mastra Documentation](https://mastra.ai/docs)

---

## ✅ Итог

После настройки:

🎯 **Cursor будет использовать библиотеку документации Mastra во всех проектах**  
📚 **Актуальная информация всегда под рукой**  
💻 **Правильные примеры кода из официальных источников**  
🚀 **Быстрая разработка с правильными практиками**  

**Готово! Теперь Cursor знает Mastra! 🎉**



