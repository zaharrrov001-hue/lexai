# 🔌 MCP (Model Context Protocol) Интеграция

## ✅ MCP подключен к LexAI Agent!

MCP (Model Context Protocol) - это открытый стандарт для подключения AI агентов к внешним инструментам и ресурсам. Это универсальная система плагинов, которая позволяет агентам вызывать инструменты независимо от языка или среды хостинга.

---

## 📦 Что было добавлено

### 1. Зависимость
```json
"@mastra/mcp": "latest"
```

### 2. MCP Client
Создан файл `src/mastra/mcp/lexai-mcp-client.ts` с подключением к:

- **Wikipedia MCP Server** - для поиска информации в Wikipedia
- **Weather Service** - для получения погодных данных (опционально, требует SMITHERY_API_KEY)

### 3. Интеграция с агентом
Агент обновлен для использования MCP инструментов через динамические toolsets.

---

## 🛠️ Что дает MCP

### Wikipedia MCP Server
**Возможности:**
- Поиск статей в Wikipedia
- Получение информации по запросам
- Доступ к энциклопедическим данным

**Пример использования:**
```typescript
// Агент может использовать Wikipedia для поиска информации
const response = await lexaiAgent.generate("Найди информацию о гражданском праве в Wikipedia", {
  toolsets: await lexaiMcpClient.getToolsets(),
});
```

### Weather Service (опционально)
**Возможности:**
- Получение текущей погоды
- Прогноз погоды
- Метеорологические данные

**Требования:**
- `SMITHERY_API_KEY` в переменных окружения (опционально)

---

## 📝 Использование

### Статический подход (рекомендуется для простых случаев)

```typescript
import { lexaiMcpClient } from "./mcp/lexai-mcp-client";

// Получить инструменты один раз
const mcpTools = await lexaiMcpClient.getTools();

// Использовать в агенте
const agent = new Agent({
  // ...
  tools: {
    ...yourTools,
    ...mcpTools,
  },
});
```

### Динамический подход (для multi-tenant приложений)

```typescript
import { lexaiMcpClient } from "./mcp/lexai-mcp-client";
import { lexaiAgent } from "./agents/lexai-agent";

// Использовать toolsets при генерации
const response = await lexaiAgent.generate("Найди информацию о праве", {
  toolsets: await lexaiMcpClient.getToolsets(),
});
```

---

## 🔧 Конфигурация

### Переменные окружения

Добавьте в `.env` (опционально):

```env
# Для Weather Service (опционально)
SMITHERY_API_KEY=your-smithery-api-key
```

### Настройка MCP серверов

В `src/mastra/mcp/lexai-mcp-client.ts` можно добавить больше серверов:

```typescript
export const lexaiMcpClient = new MCPClient({
  id: "lexai-mcp-client",
  servers: {
    wikipedia: {
      command: "npx",
      args: ["-y", "wikipedia-mcp"],
    },
    weather: {
      url: new URL(`https://server.smithery.ai/...`),
    },
    // Добавьте больше серверов здесь
  },
});
```

---

## 📚 Доступные MCP серверы

### Популярные реестры MCP серверов:

1. **Smithery.ai** - реестр MCP серверов
2. **mcp.run** - управляемые MCP серверы
3. **Composio.dev** - интеграции с SaaS
4. **Klavis AI** - enterprise MCP серверы
5. **Ampersand** - 150+ интеграций

### Примеры серверов:

- Wikipedia MCP ✅ (уже подключен)
- Weather Service ✅ (уже подключен)
- Google Sheets
- Gmail
- Salesforce
- HubSpot
- И многие другие...

---

## 🧪 Тестирование

### 1. Локальное тестирование

```bash
cd mastra
npm install
npm run dev
```

### 2. Тест в Studio

1. Откройте `http://localhost:4111`
2. Выберите `lexaiAgent`
3. Попробуйте запрос: "Найди информацию о гражданском праве в Wikipedia"

### 3. Тест через API

```typescript
import { lexaiAgent } from "./agents/lexai-agent";
import { lexaiMcpClient } from "./mcp/lexai-mcp-client";

const response = await lexaiAgent.generate(
  "Найди информацию о трудовом праве в Wikipedia",
  {
    toolsets: await lexaiMcpClient.getToolsets(),
  }
);

console.log(response.text);
```

---

## ⚠️ Важные замечания

1. **Wikipedia MCP** работает без дополнительных ключей
2. **Weather Service** требует `SMITHERY_API_KEY` (опционально)
3. MCP серверы запускаются через `npx` при первом использовании
4. Для production используйте кеширование инструментов

---

## 🔄 Отключение MCP

Если нужно временно отключить MCP:

```typescript
// Просто не передавайте toolsets
const response = await lexaiAgent.generate("Запрос", {
  // toolsets не переданы - MCP не используется
});
```

---

## 📖 Дополнительная информация

- [Mastra MCP Documentation](https://mastra.ai/docs/mcp/overview)
- [Model Context Protocol](https://modelcontextprotocol.io/)
- [MCP Servers Registry](https://smithery.ai/)

---

## ✅ Итог

MCP успешно подключен! Теперь LexAI Agent может:
- ✅ Искать информацию в Wikipedia
- ✅ Получать погодные данные (если настроено)
- ✅ Легко расширяться новыми MCP серверами

**Готово к использованию! 🚀**



