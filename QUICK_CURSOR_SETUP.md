# ⚡ Быстрая настройка Cursor для Mastra

## 🎯 Цель: Cursor использует библиотеку документации Mastra во всех проектах

---

## 🚀 Способ 1: Автоматическая установка (30 секунд)

### Шаг 1: Откройте настройки Cursor

1. Откройте Cursor
2. Нажмите `Cmd + ,` (Mac) или `Ctrl + ,` (Windows/Linux)
3. Найдите раздел **"MCP"** или **"Model Context Protocol"**

### Шаг 2: Добавьте конфигурацию

Скопируйте содержимое файла `cursor-mcp-config.json` из этого проекта:

```json
{
  "mcpServers": {
    "mastra-docs": {
      "command": "npx",
      "args": ["-y", "@mastra/mcp-docs-server"]
    }
  }
}
```

Вставьте в настройки MCP в Cursor.

### Шаг 3: Перезапустите Cursor

Готово! Теперь Cursor знает Mastra во всех проектах.

---

## 🔧 Способ 2: Через файл конфигурации

### Шаг 1: Найдите файл конфигурации

**macOS/Linux:**
```bash
~/.cursor/mcp.json
```

**Windows:**
```
%APPDATA%\Cursor\mcp.json
```

### Шаг 2: Добавьте конфигурацию

Создайте файл (если нет) или откройте существующий и добавьте:

```json
{
  "mcpServers": {
    "mastra-docs": {
      "command": "npx",
      "args": ["-y", "@mastra/mcp-docs-server"]
    }
  }
}
```

### Шаг 3: Перезапустите Cursor

---

## ✅ Проверка

Спросите в Cursor:

```
"Создай агента Mastra с memory"
```

Если Cursor использует документацию Mastra, он создаст правильный код с актуальным API.

---

## 🎯 Результат

После настройки:

✅ **В любом проекте** Cursor будет использовать документацию Mastra  
✅ **Актуальная информация** всегда под рукой  
✅ **Правильные примеры** из официальных источников  

---

## 📚 Подробная инструкция

См. `CURSOR_MCP_SETUP.md` для детальной информации и решения проблем.

---

**Готово! Cursor теперь знает Mastra! 🎉**



