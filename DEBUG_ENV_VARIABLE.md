# 🔍 Отладка проблемы с OPENAI_API_KEY

## ❌ Проблема в логах

```
"Не удалось найти ключ API process.env.OPENAI_API_KEY для модели с идентификатором openai/gpt-4o-mini"
```

Это означает, что переменная окружения не доступна во время выполнения.

## ✅ Пошаговая проверка и исправление

### Шаг 1: Проверьте, что переменная сохранена

1. **Откройте Project Settings → Env Variables**
2. **Проверьте список переменных:**
   - Должна быть переменная `OPENAI_API_KEY`
   - Значение должно начинаться с `sk-`
   - Должна быть отмечена как "Sensitive"

3. **Если переменной нет или неправильное имя:**
   - Удалите старую (если есть)
   - Создайте новую:
     - **Key:** `OPENAI_API_KEY` (точное имя, заглавными)
     - **Value:** ваш ключ
     - **Sensitive:** ✅
   - **Нажмите "Save variables"**

### Шаг 2: Убедитесь, что переменная применена

После сохранения переменной **обязательно** нужно перезапустить проект:

1. **Перейдите в Deployments**
2. **Найдите последний деплой** (например, `1b396657`)
3. **Нажмите на него** для просмотра деталей
4. **Нажмите "Redeploy"** или найдите кнопку перезапуска

**ВАЖНО:** Просто сохранение переменной не применяет её к уже запущенному процессу!

### Шаг 3: Проверьте логи после перезапуска

После перезапуска проверьте логи:

1. **Deployments → Logs**
2. **Ищите строку:** `API Mastra работает на порту`
3. **После этого НЕ должно быть ошибок** про `OPENAI_API_KEY`

### Шаг 4: Если ошибка все еще есть

#### Вариант A: Проверьте формат ключа

Убедитесь, что ключ:
- Начинается с `sk-` или `sk-proj-`
- Не содержит пробелов в начале/конце
- Полный (не обрезан)

#### Вариант B: Удалите и создайте заново

1. **Удалите переменную** `OPENAI_API_KEY`
2. **Сохраните** (Save variables)
3. **Подождите 10 секунд**
4. **Создайте заново:**
   - Key: `OPENAI_API_KEY`
   - Value: ваш ключ
   - Sensitive: ✅
5. **Сохраните**
6. **Перезапустите проект** (Redeploy)

#### Вариант C: Проверьте через API

Попробуйте проверить, доступна ли переменная:

1. Откройте **Deployments → API**
2. Найдите endpoint для проверки переменных окружения
3. Или создайте тестовый запрос к агенту

## 🔄 Полный цикл исправления

1. ✅ **Удалить** старую переменную (если есть)
2. ✅ **Создать** новую `OPENAI_API_KEY`
3. ✅ **Сохранить** (Save variables)
4. ✅ **Перезапустить** проект (Redeploy)
5. ✅ **Проверить** логи (не должно быть ошибок)
6. ✅ **Протестировать** агента (создать новый чат)

## ⏱️ Время ожидания

- Сохранение переменной: мгновенно
- Перезапуск проекта: 1-3 минуты
- После перезапуска переменная должна быть доступна

## 📋 Чеклист

- [ ] Переменная `OPENAI_API_KEY` существует в настройках
- [ ] Имя точно `OPENAI_API_KEY` (не `openai1`, не `OPENAI_KEY`)
- [ ] Значение полное и правильное
- [ ] Переменная сохранена (Save variables нажат)
- [ ] Проект перезапущен после сохранения
- [ ] В логах нет ошибок про `OPENAI_API_KEY`
- [ ] Агент работает без предупреждений

## 🚨 Если ничего не помогает

1. **Проверьте документацию Mastra Cloud:**
   - Возможно, есть особенности применения переменных
   - Может быть задержка в применении

2. **Создайте новый деплой через Git:**
   ```bash
   git commit --allow-empty -m "Force redeploy with OPENAI_API_KEY"
   git push origin main
   ```

3. **Обратитесь в поддержку Mastra Cloud:**
   - Опишите проблему
   - Приложите логи
   - Укажите, что переменная создана, но не применяется

