# 🔧 Исправление ошибки MCP конфигурации в Cursor

## ❌ Ошибка

```
MCP configuration errors: cursor-ide-browser: JSON syntax error: 
Unexpected non-whitespace character after JSON at position 1870 (line 83 column 2)
```

## ✅ Решение

### Шаг 1: Откройте JSON конфигурацию

В настройках Cursor нажмите кнопку **"Open JSON"** рядом с ошибкой.

Или найдите файл конфигурации вручную:

**macOS:**
```bash
~/.cursor/mcp.json
# или
~/Library/Application Support/Cursor/User/globalStorage/mcp.json
```

**Windows:**
```
%APPDATA%\Cursor\User\globalStorage\mcp.json
```

**Linux:**
```bash
~/.config/Cursor/User/globalStorage/mcp.json
```

### Шаг 2: Проверьте синтаксис JSON

Ошибка на **строке 83, позиция 1870**. Проверьте:

1. **Лишние запятые** - не должно быть запятой после последнего элемента
2. **Незакрытые скобки** - все `{` должны иметь `}`, все `[` должны иметь `]`
3. **Кавычки** - все строки должны быть в двойных кавычках `"`, не одинарных
4. **Комментарии** - JSON не поддерживает комментарии `//` или `/* */`

### Шаг 3: Исправьте конфигурацию

Вот **правильный формат** для добавления Mastra Docs:

```json
{
  "mcpServers": {
    "cursor-ide-browser": {
      "command": "node",
      "args": ["path/to/browser/server.js"]
    },
    "mastra-docs": {
      "command": "npx",
      "args": ["-y", "@mastra/mcp-docs-server"]
    }
  }
}
```

### Шаг 4: Проверьте JSON валидность

Используйте онлайн валидатор или команду:

```bash
# Проверка JSON
cat ~/.cursor/mcp.json | python3 -m json.tool
```

Или используйте онлайн: https://jsonlint.com/

### Шаг 5: Перезапустите Cursor

1. Полностью закройте Cursor
2. Откройте заново
3. Ошибка должна исчезнуть

---

## 🔍 Частые проблемы

### Проблема 1: Лишняя запятая

**Неправильно:**
```json
{
  "mcpServers": {
    "server1": {...},
    "server2": {...},  // ← лишняя запятая!
  }
}
```

**Правильно:**
```json
{
  "mcpServers": {
    "server1": {...},
    "server2": {...}
  }
}
```

### Проблема 2: Комментарии в JSON

**Неправильно:**
```json
{
  // Это комментарий - НЕ РАБОТАЕТ в JSON!
  "mcpServers": {...}
}
```

**Правильно:**
```json
{
  "mcpServers": {...}
}
```

### Проблема 3: Одинарные кавычки

**Неправильно:**
```json
{
  'mcpServers': {...}  // ← одинарные кавычки!
}
```

**Правильно:**
```json
{
  "mcpServers": {...}  // ← двойные кавычки!
}
```

---

## 🎯 Минимальная рабочая конфигурация

Если ничего не помогает, используйте минимальную конфигурацию:

```json
{
  "mcpServers": {
    "mastra-docs": {
      "command": "npx",
      "args": ["-y", "@mastra/mcp-docs-server"]
    }
  }
}
```

Сохраните и перезапустите Cursor.

---

## ✅ Проверка

После исправления:

1. Откройте настройки Cursor: `Cmd/Ctrl + ,`
2. Перейдите в **"Tools & MCP"**
3. Ошибка должна исчезнуть
4. Должен появиться **"mastra-docs"** в списке MCP серверов

---

## 📞 Если не помогло

1. Удалите файл конфигурации MCP (создастся заново)
2. Добавьте только Mastra Docs конфигурацию
3. Перезапустите Cursor

---

**После исправления Cursor будет использовать библиотеку документации Mastra! 🎉**


