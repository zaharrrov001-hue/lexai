# LexAI Agent для Mastra Cloud

Полнофункциональный AI агент для юридических задач, анализа документов и информационного поиска, развернутый на Mastra Cloud.

## 🚀 Возможности

- **Веб-поиск**: Поиск актуальной информации в интернете
- **Анализ документов**: Обработка PDF, DOCX, TXT и других форматов
- **Юридические запросы**: Обработка правовых вопросов и консультаций
- **Профессиональные ответы**: Структурированные и точные ответы на русском языке
- **MCP интеграция**: Доступ к библиотеке документации Mastra, Wikipedia, и другим сервисам

## 🎯 Настройка Cursor для работы с Mastra

**Важно:** Чтобы Cursor использовал библиотеку документации Mastra во всех проектах, настройте MCP сервер:

### Быстрая установка (рекомендуется):

1. **Автоматическая установка:** Откройте файл `cursor-mcp-config.json` в этом проекте и скопируйте его содержимое в настройки Cursor MCP

2. **Или используйте кнопку:** См. подробную инструкцию в `CURSOR_MCP_SETUP.md`

### После настройки:

✅ Cursor будет использовать актуальную документацию Mastra во всех проектах  
✅ Правильные примеры кода из официальных источников  
✅ Знание последних изменений в API Mastra  

📖 **Подробная инструкция:** См. `CURSOR_MCP_SETUP.md`

## 📋 Требования

- Node.js 20 или выше
- API ключ от одного из провайдеров:
  - OpenAI (рекомендуется)
  - Google Gemini
  - Anthropic Claude
  - Или любой другой поддерживаемый провайдер из [списка Mastra](https://mastra.ai/models)

## 🛠️ Установка

1. **Установите зависимости:**

```bash
npm install
```

2. **Настройте переменные окружения:**

Скопируйте `.env.example` в `.env` и добавьте ваш API ключ:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
OPENAI_API_KEY=your-api-key-here
```

3. **Запустите в режиме разработки:**

```bash
npm run dev
```

Это запустит Mastra dev сервер на `http://localhost:4111`

## 🌐 Развертывание на Mastra Cloud

### Шаг 1: Подготовка проекта

Убедитесь, что проект собран:

```bash
npm run build
```

### Шаг 2: Подключение к Mastra Cloud

1. Откройте [Mastra Cloud](https://cloud.mastra.ai)
2. Войдите в свой аккаунт
3. Создайте новый проект или выберите существующий

### Шаг 3: Настройка подключения

В интерфейсе Mastra Cloud:

1. **URL-адрес Mastra API**: 
   - Если развертываете локально: `http://localhost:4111`
   - Если на сервере: `https://your-domain.com` или `http://your-ip:4111`

2. **Заголовки запросов** (если требуется):
   - `Authorization: Bearer YOUR_API_KEY` (если используете защиту)
   - Другие необходимые заголовки

3. Нажмите **"Создать проект"**

### Шаг 4: Использование агента

После подключения вы можете:

- Использовать агента через Mastra Cloud UI
- Вызывать через API:
  ```bash
  curl -X POST http://localhost:4111/api/agents/lexaiAgent/generate \
    -H "Content-Type: application/json" \
    -d '{
      "messages": [
        {
          "role": "user",
          "content": "Проанализируй этот документ..."
        }
      ]
    }'
  ```

## 📁 Структура проекта

```
mastra/
├── src/
│   └── mastra/
│       ├── agents/
│       │   └── lexai-agent.ts      # Основной агент
│       ├── tools/
│       │   ├── web-search-tool.ts      # Инструмент веб-поиска
│       │   ├── document-analysis-tool.ts # Инструмент анализа документов
│       │   ├── legal-query-tool.ts      # Инструмент юридических запросов
│       │   └── index.ts                 # Экспорт всех инструментов
│       └── index.ts                 # Mastra instance
├── package.json
├── tsconfig.json
├── .env.example
└── README.md
```

## 🔧 Настройка агента

### Изменение модели

В `src/mastra/agents/lexai-agent.ts` измените:

```typescript
model: "openai/gpt-4o-mini", // На другую модель, например:
// model: "google/gemini-2.5-pro",
// model: "anthropic/claude-3-5-sonnet",
```

### Добавление новых инструментов

1. Создайте новый файл в `src/mastra/tools/`
2. Экспортируйте из `src/mastra/tools/index.ts`
3. Добавьте в `tools` объекта агента в `lexai-agent.ts`

### Настройка инструкций

Отредактируйте поле `instructions` в `lexai-agent.ts` для изменения поведения агента.

## 🧪 Тестирование

### Через Mastra Studio

1. Запустите `npm run dev`
2. Откройте [http://localhost:4111](http://localhost:4111)
3. Выберите `lexaiAgent` в интерфейсе
4. Протестируйте различные запросы

### Примеры запросов

- "Найди информацию о последних изменениях в трудовом законодательстве"
- "Проанализируй этот документ и выдели ключевые моменты"
- "Какие риски есть в этом договоре?"

## 📚 Документация

- [Mastra Documentation](https://mastra.ai/docs)
- [Mastra Agents Guide](https://mastra.ai/docs/agents/overview)
- [Mastra Tools Guide](https://mastra.ai/docs/agents/using-tools)

## 🔐 Безопасность

- Никогда не коммитьте `.env` файл в Git
- Используйте переменные окружения для API ключей
- Настройте CORS и аутентификацию для production

## 📝 Лицензия

Этот проект является частью LexAI системы.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте документацию Mastra
2. Убедитесь, что API ключ настроен правильно
3. Проверьте логи в консоли

---

**Готово к развертыванию на Mastra Cloud! 🚀**

