# ✅ Быстрый чеклист развертывания на Mastra Cloud

## 🚀 Шаги (5 минут):

### 1. Откройте Mastra Cloud
- https://cloud.mastra.ai/
- Войдите в аккаунт

### 2. Подключите GitHub репозиторий
- Создайте/откройте проект
- Settings → Connect GitHub
- Repository: `zaharrrov001-hue/lexai`
- Branch: `main`
- Project root: `.`
- Mastra directory: `src/mastra`

### 3. Добавьте OPENAI_API_KEY ⚠️ ОБЯЗАТЕЛЬНО
- Settings → Environment Variables
- Name: `OPENAI_API_KEY`
- Value: `sk-...` (ваш ключ OpenAI)
- Сохраните

### 4. Дождитесь развертывания
- Mastra Cloud автоматически:
  - Клонирует код
  - Установит зависимости
  - Соберет проект
  - Запустит приложение

### 5. Проверьте статус
- Должно быть: ✅ Build successful
- Readiness probe: ✅ Passed
- Если ошибки - проверьте логи

## 🔧 Автоматическая настройка (опционально):

```bash
cd mastra
export OPENAI_API_KEY="sk-your-key-here"
node setup-mastra-cloud.js
```

## ❌ Если ошибка "API key not found":
→ Добавьте `OPENAI_API_KEY` в Environment Variables

## ❌ Если ошибка "Readiness probe failed":
→ Проверьте логи, убедитесь что `OPENAI_API_KEY` установлен

## ✅ Готово!
После успешного развертывания агент будет доступен через API Mastra Cloud.

