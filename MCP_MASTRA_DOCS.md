# 📚 Mastra Docs MCP Server - Доступ к библиотеке документации

## ✅ Добавлен Mastra Docs MCP Server!

Теперь ваш агент имеет **прямой доступ к полной библиотеке знаний Mastra** через MCP протокол!

---

## 🎯 Что это дает

### Mastra Docs MCP Server предоставляет доступ к:

1. **📖 Полная документация Mastra**
   - Все руководства и туториалы
   - API референсы
   - Примеры использования

2. **💻 Примеры кода**
   - Готовые примеры для разных сценариев
   - Best practices
   - Паттерны использования

3. **📝 Блог-посты**
   - Последние новости и обновления
   - Гайды и статьи
   - Кейсы использования

4. **📋 Changelogs**
   - История изменений
   - Новые функции
   - Breaking changes

---

## 🚀 Преимущества для агента

### Агент теперь может:

✅ **Искать в документации Mastra** - находить актуальную информацию о фреймворке  
✅ **Использовать примеры кода** - получать готовые примеры для решения задач  
✅ **Узнавать о новых функциях** - быть в курсе последних обновлений  
✅ **Правильно использовать API** - иметь доступ к актуальной документации API  

### Примеры использования:

```
Пользователь: "Как добавить memory к агенту в Mastra?"
Агент: Использует Mastra Docs MCP → находит документацию → предоставляет актуальную информацию
```

```
Пользователь: "Покажи пример создания workflow в Mastra"
Агент: Использует Mastra Docs MCP → находит примеры кода → показывает готовый пример
```

```
Пользователь: "Какие новые функции появились в последней версии Mastra?"
Агент: Использует Mastra Docs MCP → проверяет changelog → предоставляет информацию
```

---

## 🔧 Технические детали

### Как это работает:

1. **MCP Client** подключается к `@mastra/mcp-docs-server`
2. **Сервер** предоставляет доступ к базе знаний Mastra
3. **Агент** может запрашивать информацию через MCP протокол
4. **Результаты** возвращаются с актуальной документацией

### Конфигурация:

```typescript
mastraDocs: {
  command: "npx",
  args: ["-y", "@mastra/mcp-docs-server"],
}
```

Сервер запускается автоматически через `npx` при первом использовании.

---

## 💡 Практические примеры

### Пример 1: Поиск в документации
```typescript
const response = await lexaiAgent.generate(
  "Как использовать memory в Mastra? Покажи пример",
  {
    toolsets: await lexaiMcpClient.getToolsets(),
  }
);
// Агент использует Mastra Docs MCP для поиска актуальной информации
```

### Пример 2: Получение примеров кода
```typescript
const response = await lexaiAgent.generate(
  "Покажи пример создания workflow с несколькими шагами в Mastra",
  {
    toolsets: await lexaiMcpClient.getToolsets(),
  }
);
// Агент получает готовый пример из документации
```

### Пример 3: Проверка API
```typescript
const response = await lexaiAgent.generate(
  "Какие параметры можно передать в Agent.generate()?",
  {
    toolsets: await lexaiMcpClient.getToolsets(),
  }
);
// Агент обращается к API документации
```

---

## 📊 Что теперь доступно агенту

### Через Mastra Docs MCP:

| Тип информации | Доступность | Примеры |
|---------------|-------------|---------|
| **Документация** | ✅ Полная | Руководства, туториалы, API референсы |
| **Примеры кода** | ✅ Все примеры | Готовые примеры для разных сценариев |
| **Блог-посты** | ✅ Все статьи | Новости, гайды, кейсы |
| **Changelogs** | ✅ История | Новые функции, изменения |

---

## 🎯 Итоговый результат

### Ваш агент теперь:

✅ **Знает Mastra** - имеет доступ к полной документации  
✅ **Использует актуальную информацию** - всегда в курсе последних изменений  
✅ **Может показать примеры** - имеет доступ к готовым примерам кода  
✅ **Правильно использует API** - знает актуальные параметры и методы  

### Это особенно полезно для:

- 🔧 Разработки новых функций
- 🐛 Отладки проблем
- 📚 Изучения возможностей Mastra
- 🚀 Оптимизации кода
- 🔄 Обновления до новых версий

---

## 📖 Дополнительная информация

- [Mastra Docs MCP Server](https://mastra.ai/docs/getting-started/mcp-docs-server)
- [MCP Overview](https://mastra.ai/docs/mcp/overview)
- [Mastra Documentation](https://mastra.ai/docs)

---

**Mastra Docs MCP Server подключен! Агент теперь имеет доступ к полной библиотеке документации Mastra! 🎉**



